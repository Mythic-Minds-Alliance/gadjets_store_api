import { ProductModel } from './product.model';

import { UserModel } from './users.roles.model';
import { RoleModel } from './user.model';

const models = [RoleModel, UserModel, ProductModel];

module.exports = models;
